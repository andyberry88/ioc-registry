{
  "name": "ioc-registry",
  "version": "0.0.1",
  "description": "A registry which allows a module implementation and objects to be registered against a logical name to provide an IOC mechanism.",
  "main": "index.js",
  "scripts": {
	"lint": "jshint src tests",
    "pretest": "npm install -g mocha jshint",
    "test": "mocha tests",
    "posttest": "npm run lint",
    "test-dev": "mocha tests -w"
  },
  "author": "Andy Berry <http://andy-berry.co.uk>",
  "license": "MIT",
  "dependencies": {
    "sprintf-js": "^1.0.3",
    "topiarist": "^0.1.4"
  },
  "devDependencies": {
    "chai": "^3.3.0",
    "mocha": "^2.3.3"
  }
}
